// import { RequestData, dretch, parse } from './main'
// // todo
// const r1 = ['GET', '/todos'] as RequestData
// const r2: RequestData = ['POST', '/users', { body: { name: 'John' } }]

// type Task = {
//   title: string
// }

// const request = dretch('https://jsonplaceholder.typicode.com/')
// const r = await request(...r1)
// const tasks = await parse<Task[]>(r)
// // console.log(await r.json())
// // const post = dretch(...r2)
